# GitHub setup

**Description:** Segmented enterprise web/security infrastructure using pfSense, Active Directory, BIND, Exchange/Postfix, Apache, ADC load balancing, PKI, DNSSEC, TLS, and email authentication.

**Topics:** `cybersecurity` `systems-administration` `pfsense` `active-directory` `bind` `dnssec` `openssl` `pki` `apache` `postfix` `exchange` `dmarc` `dkim` `spf` `load-balancing`

```bash
git init
git add .
git commit -m "Initial portfolio release"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/enterprise-web-security-infrastructure.git
git push -u origin main
```
